// 2. Cree un programa que imprima la tabla de multiplicar del 3, del 0 al 50. Use ciclo for. Use funciones.

function tablaMultiplicar() {
    for (let index = 3; index <= 3; index++) {
        for (let j = 0;  j <=50 ; j++) {
            let mul= index * j;
            console.log(`3x${j}=${mul}`);
        }
    }
}

tablaMultiplicar();
